# Alert Engine

Phase 12 alert records include scope, rule ID, severity, status, fingerprint, message, payload, acknowledgement, and resolution timestamps.

Deduplication is represented by alert fingerprints. Deterministic actions can warn, block new paper submissions, pause strategy deployment, or set portfolio emergency state. Phase 12 does not close positions automatically.
