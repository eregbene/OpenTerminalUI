# Reproducibility

Each completed workflow exports a manifest with app/runtime, strategy hash, dataset hash, execution/cost models, optimization and validation jobs, scorecard, candidate and artifact hashes.
