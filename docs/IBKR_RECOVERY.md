# IBKR Recovery

On reconnect, the system must reload open orders and recent executions, match them to OMS records, and mark ambiguous submissions for review.

It must never resubmit unresolved orders automatically.
