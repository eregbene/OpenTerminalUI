# Research UI

`/backtesting/research-lab` adds the canonical strategy validation workflow.

It shows configuration, backtest metrics, optimization trials, validation folds, robustness, scorecard gates, candidate status and reproducibility manifest.

The UI explicitly states that research qualification does not activate trading.
