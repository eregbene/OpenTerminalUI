# Strategy UI

The Chart Workstation includes a `STRAT` overlay toggle beside existing chart overlay controls.

The inspector:

- lists registered deterministic strategies
- evaluates the selected strategy against visible chart bars
- shows the latest decision
- shows proposal reference, invalidation, and target levels
- displays warnings and evaluation errors

The UI does not add order placement or broker actions.
