# Wyckoff Framework

The Wyckoff framework is registered but currently limited-data.

It requires reliable range phase detection, cause/effect measurements, volume context, springs, upthrusts, and tests. Until those inputs are implemented deterministically, the framework returns `INSUFFICIENT_DATA` with missing evidence.
