# Walk-Forward Validation

Implemented rolling and anchored fold plans by bar counts.

Each fold records train range, validation range, selected parameters, train metrics, validation metrics and degradation. Purge and embargo bars are modeled and applied between train and validation slices.
