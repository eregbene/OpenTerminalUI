# SMC Configuration

Configuration model: `MarketStructureConfig`.

Profiles:

- `internal`: faster swing sensitivity
- `balanced`: default
- `external`: slower swing sensitivity and stricter break confirmation

Every result stores:

- engine name
- version
- full configuration payload
- stable configuration hash

There are no hidden global parameters.
