# Research Testing

Focused tests:

```powershell
python -m pytest backend/tests/test_research_phase7_workflow.py -q
npm.cmd test -- ResearchWorkflowPage.test.tsx --run
```

Coverage includes next-bar timing, conservative fills, costs/metrics, optimization reproducibility, validation, scorecards, candidates, workflow lineage and API.
