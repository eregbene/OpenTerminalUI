# Multi-Objective Optimization

The vertical slice ranks by one configured objective and records Pareto placeholders from ranked trials.

Future work can add true multi-objective ranking without changing trial lineage.
