# Displacement

Displacement is detected with measurable candle properties:

- body size relative to ATR
- total range relative to ATR
- body-to-range ratio
- optional volume ratio

The first implementation supports single-bar displacement. Consecutive-bar displacement is represented in configuration and may be expanded later.

Volume remains optional because OHLCV feeds may lack true volume.
