# Optimization Engine

Implemented methods:

- grid search
- random search with deterministic seed

Every trial is retained, including failed trials. Parameter paths are allow-listed and cannot mutate arbitrary strategy configuration.
