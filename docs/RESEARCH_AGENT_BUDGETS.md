# Research Agent Budgets

Research budgets enforce:

- job count
- active job count
- estimated provider tokens
- estimated provider cost
- task graph size

When a hard budget is reached, new launches are blocked and the plan may be marked `PAUSED_BUDGET`.
