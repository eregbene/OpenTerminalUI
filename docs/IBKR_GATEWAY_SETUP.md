# IBKR Gateway Setup

Use IB Gateway paper mode. Enable API access and use a dedicated client ID.

Do not reuse a client ID already used by another automation. Duplicate client ID and API-disabled states must surface as broker health problems.
