# Strategy Ranking

The Phase 12 UI includes a Strategy Leaderboard placeholder that refuses to rank strategies until allocation and performance samples exist.

Ranking must remain deterministic, versioned, and transparent. Missing data must penalize or disqualify rather than fabricate comparability.
